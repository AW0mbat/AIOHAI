"""
Screen Capture — Future: Capture screen content for AI contextual awareness.

This module will provide:
- Screen capture triggering (user-initiated or AI-requested)
- OCR pipeline (Tesseract) for text extraction
- PII scanning of OCR'd text before LLM sees it
- Ephemeral storage (captures processed and discarded, not persisted)

Core services consumed:
- core.analysis.pii_protector.PIIProtector
- core.audit.transparency.SessionTransparencyTracker

Status: PLACEHOLDER
"""

# Placeholder - implementation is future work
