# Core dependencies
requests>=2.28.0
psutil>=5.9.0

# HSM Integration (Nitrokey HSM 2)
# Requires OpenSC to be installed on system
PyKCS11>=1.5.12

# FIDO2/WebAuthn (Nitrokey 3A NFC) - Stage 2
fido2>=1.1.0

# Web server for mobile approval interface - Stage 2
flask>=3.0.0
flask-cors>=4.0.0

# Cryptography
cryptography>=42.0.0

# Optional: Windows-specific
# pywin32>=306  # Uncomment on Windows for full functionality
