# AIOHAI v5.0.0 — Dependencies
# Install: pip install -r requirements.txt
# Entry point: python -m aiohai

# Core dependencies
requests>=2.28.0
psutil>=5.9.0

# HSM Integration (Nitrokey HSM 2)
# Requires OpenSC to be installed on system
PyKCS11>=1.5.12

# FIDO2/WebAuthn (Nitrokey 3A NFC)
fido2>=1.1.0

# Web server for mobile approval interface
flask>=3.0.0
flask-cors>=4.0.0

# Cryptography
cryptography>=42.0.0

# Optional: Windows-specific
# pywin32>=306  # Uncomment on Windows for full functionality

# Optional: Office document support (metadata sanitization, content scanning)
# Required only if office.enabled=true in config.json
# python-docx>=0.8.11  # Word document support
# openpyxl>=3.1.0      # Excel document support
# python-pptx>=0.6.21  # PowerPoint document support

# Development / Testing
# pytest>=7.0.0
# pytest-cov>=4.0.0
